from random import choice
print("что вы хотите отредактировать:")
y = input()
x = choice = int(input("\nВведите номер (10-1000"  + "): "))


def corrector(string, width, symbol):
    new_string = string.center(width, symbol)
    return new_string

result = corrector(y, x, "*")
print(result)

